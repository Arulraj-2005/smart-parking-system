FROM node:18-alpine

WORKDIR /app

# Copy package files (they are in the root of Parking_frontend, not in server folder)
COPY package*.json ./

# Install dependencies
RUN npm install --only=production

# Copy server code
COPY server/ ./server/

# Expose port
EXPOSE 5000

# Start backend
CMD ["node", "server/index.js"]